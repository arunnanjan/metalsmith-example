{
  "name": "mysite",
  "version": "1.0.0",
  "description": "A blog",
  "main": "index.js",
  "scripts": {
    "server": "node index.js",
    "build": "node build.js",
    "start": "npm run build && npm run server",
    "run-watch": "nodemon -e json,md,pug,scss --ignore build/ --exec npm run start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.14.0",
    "metalsmith": "^2.2.0",
    "metalsmith-layouts": "^1.6.5",
    "metalsmith-markdownit": "^0.4.0",
    "metalsmith-sass": "^1.3.0",
    "pug": "^2.0.0-beta6",
    "serve-static": "^1.11.1"
  }
}
